# Gmail SMTP 설정 가이드

네트워크 모니터링 시스템에서 Gmail을 사용한 이메일 알림을 설정하는 방법을 안내합니다.

## 🔐 Gmail 앱 비밀번호 생성하기

Gmail SMTP를 사용하려면 일반 비밀번호가 아닌 **앱 비밀번호**를 사용해야 합니다.

### 1단계: 2단계 인증 활성화

1. [Google 계정 설정](https://myaccount.google.com/)에 접속
2. **보안** 탭 클릭
3. **2단계 인증** 섹션에서 **2단계 인증 사용** 클릭
4. 휴대폰 번호를 입력하고 인증 완료

### 2단계: 앱 비밀번호 생성

1. 2단계 인증이 활성화된 후 **앱 비밀번호** 섹션으로 이동
2. **앱 비밀번호** 클릭
3. **앱 선택** 드롭다운에서 **메일** 선택
4. **기기 선택** 드롭다운에서 **기타(맞춤 이름)** 선택
5. "네트워크 모니터링 시스템" 입력
6. **생성** 버튼 클릭
7. **16자리 앱 비밀번호** 복사 (예: `abcd efgh ijkl mnop`)

## ⚙️ 시스템 설정

### 1. 환경 변수 설정

`.env` 파일을 열고 다음 정보를 입력하세요:

```env
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password
PORT=3000
NODE_ENV=development
```

### 2. 웹 인터페이스에서 설정

1. 네트워크 모니터링 시스템 실행
2. **설정** 버튼 클릭
3. 다음 정보 입력:
   - **SMTP 서버**: `smtp.gmail.com`
   - **SMTP 포트**: `587`
   - **사용자명**: `your-email@gmail.com`
   - **비밀번호**: `앱 비밀번호` (16자리)
   - **발신자 이메일**: `your-email@gmail.com`
   - **수신자 이메일**: `admin@company.com, ops@company.com`

### 3. 연결 테스트

설정 완료 후 **연결 테스트** 버튼을 클릭하여 설정이 올바른지 확인하세요.

## 🔧 다른 이메일 제공업체 설정

### Outlook/Hotmail
```
SMTP 서버: smtp-mail.outlook.com
포트: 587
인증: 일반 비밀번호 사용
```

### Yahoo Mail
```
SMTP 서버: smtp.mail.yahoo.com
포트: 587
인증: 앱 비밀번호 사용 (Gmail과 동일한 방식)
```

### 기타 이메일 제공업체
각 제공업체의 SMTP 설정을 확인하세요:
- **포트**: 587 (TLS) 또는 465 (SSL)
- **보안**: STARTTLS 또는 SSL/TLS
- **인증**: 사용자명/비밀번호 또는 앱 비밀번호

## 🚨 문제 해결

### 일반적인 오류

1. **"인증 실패" 오류**
   - 2단계 인증이 활성화되어 있는지 확인
   - 앱 비밀번호를 올바르게 입력했는지 확인
   - 일반 비밀번호가 아닌 앱 비밀번호를 사용했는지 확인

2. **"연결 거부" 오류**
   - SMTP 서버 주소가 올바른지 확인
   - 포트 번호가 올바른지 확인 (587 또는 465)
   - 방화벽이 SMTP 포트를 차단하지 않는지 확인

3. **"타임아웃" 오류**
   - 네트워크 연결 상태 확인
   - 이메일 제공업체의 서버 상태 확인

### 로그 확인

문제가 발생하면 시스템 로그를 확인하세요:
1. **로그** 버튼 클릭
2. **오류** 필터 선택
3. 이메일 관련 오류 메시지 확인

## 📧 이메일 템플릿

시스템에서 발송되는 이메일은 다음과 같은 형식입니다:

### 테스트 이메일
- **제목**: `[테스트] 네트워크 모니터링 시스템 - 이메일 설정 확인`
- **내용**: 설정 정보 및 확인 메시지

### 알림 이메일
- **제목**: `[ALERT] 호스트명 (IP주소) - 네트워크 장애`
- **내용**: 호스트 정보, 알림 내용, 권장 조치

## 🔒 보안 고려사항

1. **앱 비밀번호 보안**
   - 앱 비밀번호를 안전하게 보관
   - 정기적으로 앱 비밀번호 갱신
   - 사용하지 않는 앱 비밀번호는 삭제

2. **환경 변수 보안**
   - `.env` 파일을 버전 관리에 포함하지 않음
   - 프로덕션 환경에서는 환경 변수로 설정

3. **네트워크 보안**
   - HTTPS를 사용하여 통신 암호화
   - 방화벽 설정으로 불필요한 포트 차단

## 📞 지원

문제가 지속되면 다음을 확인하세요:
1. Gmail 계정의 보안 설정
2. 네트워크 연결 상태
3. 시스템 로그의 상세 오류 메시지
4. 이메일 제공업체의 서비스 상태

---

**참고**: 이 가이드는 Gmail을 기준으로 작성되었습니다. 다른 이메일 제공업체를 사용하는 경우 해당 제공업체의 SMTP 설정을 확인하세요.